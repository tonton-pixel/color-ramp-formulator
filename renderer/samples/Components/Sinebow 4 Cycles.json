{
    "colorRamp":
    {
        "name": "Sinebow 4 Cycles",
        "formula": "rgb_t\n(\n    pow (cos (((t * 4) - 0) * PI), 2),\n    pow (cos (((t * 4) - 1/3) * PI), 2),\n    pow (cos (((t * 4) - 2/3) * PI), 2)\n)"
    }
}